# 0.30.0 pre2+ 结界归档（aborted）

2026-09-13 维护者决定：0.30.0 保留 AI 符卡及必要修复，结界延期到 0.31.0 再开。
原 `0.30.0pre2` 改名为 `aborted/0.30.0pre2`，保留 `364003cf6`；
原 `feat/0.30.0-barrier-completion` 改名为 `aborted/0.30.0-post-pre2-barrier`，保存后续源码和资产。
这是中止的开发快照，不是发布完成声明，也不表示 0.31.0 已重开。
pre3/pre4 历史包与 pre5 检查目录一并标为 aborted；版本字段不因归档改为新的 pre 或正式版本。

## 已保存的进展

- 结界复用 laser 的载体、mover、展开/收缩和同步；单平面、XYZ 顶点/三角面及独立经纬线/曲线细分。
- 结界完整工作区、共享控制栏、魔法阵右侧属性/JSON、Dock 保留、资源化本地化。
- 顶点/面文件夹和当前项下拉、自定义预设的创建/保存/删除、屏外文件夹标签跳转。
- 世界实体的吸附/推开/隔离规则、原版选择器、Codec/Analyzer/预览接线。
- 共享不可变几何、预编译包围盒树和变换后的包围盒更新；普通实体通道明确排除妖归弹幕/激光。

具体实现、未落实的虚拟弹幕交互/预计算设计和验收表保留在
[0.31.0 待重开计划](0.31.0-barrier-development-plan.md)，尤其第 3.4、3.5 节及 E/F 清单。
弹幕效果和跨实例范围尚未定案；文档中的推进假设不能当作已确认的产品决定。

## 0.31.0 重开前必须知道

- **平面软推力的落地副作用仍未完成验证**：归档保存了 `BarrierRuntime`、`BarrierEntityCollisionMixin`
  的两阶段位移处理和 `INVOKE_ASSIGN` 钩子，意图在落地伤害/反弹前调整比较基准；另保存
  `BarrierPhysicsTest` 的垂直滑动精度用例。这三项不能当成完成的修复。
  两个生产文件的窄编译用时 2.50 秒通过；随后测试类编译因 `EditorDockLayoutTest` 的
  `DockNode`/`DockPanel` 等符号无法解析而失败（54 项错误），新增用例尚未执行。
  下次应先修复窄验证入口，再检查纯软力、靠近但未着地、真实着地、软硬不同轴和微小垂直碰撞。
- 移动结界的旋转扫掠是前后变换间的直线近似，不是任意高速旋转的精确 CCD；复杂交角滑动尚未实机验证。
- 妖归虚拟弹幕/激光专用规则、施法实例分组、批量同步和轨迹片段预计算尚未接通；几何预编译不等于此功能完成。
- 最近窄生产编译 7.39 秒、窄测试类编译 4.00 秒；物理 2886、运行时 71、本地化 2365 项检查于 5.83 秒通过。
  之后只补了能力预算面板的结界标签和受力候选的类型防护，这两处尚未重编译。
- 旧成功结果另有 laser 契约 235、几何 2175、移动钩子目标 22、Dock 30；没有启动游戏或验证网络/GPU。
- 新中文键仍在分片源，尚未再次运行 `organizeLang`；不要手改扁平中文文件补齐。
- 最新结界历史包是 `build/libs/youkaishomecoming-2.7.0+HCDRS_0.30.0pre4.jar`，已 aborted，不含本轮运行时/预设/索引增量；
  SHA-256 为 `8f583f57351f373c9d85e17bc3ffeb4e0cc1c0981b3ade019c2f7f2949b70e74`。
  `build/spell-0.30.0pre5/` 保存窄编译脚本和日志，仅是本地验证目录，尚无 pre5 发布包。

## 0.30.0 与归档历史的边界

`0.30.0pre1fix` 从 `fc5a85081` 建立，以下四项独立修复已完成，不包含结界：

1. `67a925ba0`：注册 LLM purpose、按真实路由估算计费预算。
2. `6fa2f1631`：服务端统一真实可用符卡的战斗入场资格。
3. `574f34306`：AI 后台请求、剪贴板交付、10,000 token 默认预算与修改对比。
4. `c702cf316`：覆盖另一个已注册 spell ID 前确认。

它们曾回移到结界分支，对应 `8078243bc`、`ac99f2751`、`e0fa31add`、`f18d51887`；
归档保留这些提交，但 0.31.0 不能再次重复迁回。当前 0.30.0 开发线从 pre1fix 快进继续，
内置 Codex 符卡创作 skill 提示词属于 0.30.0 的后续工作。

0.31.0 重开时，从最终 0.30.0 基线建立新开发分支，逐主题迁回 `fc5a85081..fd823f196`
的结界实现及本归档末尾未完成物理改动，逐项复核与当时编辑器/LLM 修复的交界；保留资产，
重新生成中文并验证。不得直接把整个 aborted 分支合并回 0.30.0，或恢复旧 `ll_version`。
本地候选仍只用 `-Pll_version=...` 构建；不提交预发布版本号，不推送或发布未验收产物。
