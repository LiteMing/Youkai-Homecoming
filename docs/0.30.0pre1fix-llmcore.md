# pre1fix：Skill 提示词、JSON 反馈修订与异步交付

0.30.0 专注 AI 符卡和编辑器交付。结界 pre2 及之后已标为 aborted，延期到 0.31.0 再开，
见 [版本范围](0.30.0-ai-development-plan.md)。修复在 `0.30.0pre1fix` 开发，构建使用
`-Pll_version=2.7.0+HCDRS_0.30.0pre1fix`，不提交 pre 版本 bump、不推送发布。

## 玩家流程

提交新建/修改要求后即可关闭请求界面或编辑器。后台使用内置 Skill 生成完整 JSON，调用与
Raw JSON 面板相同的检查器；有问题时，将上一稿和具体字段/坏节点诊断反馈给模型修订。
首次检查通过即交付。`spell_ai.maxRevisions` 默认 2，即首稿后最多再请求两次，范围 0–5；
0 表示交付首稿和检查反馈。所有新的默认配置值只在 `YHModConfig` 定义。

完成后，客户端在主线程复制原文并发送聊天提醒。修订次数耗尽，或已有草稿后 provider 失败，
仍交付最后一份非空草稿，并提供“悬停查看检查反馈”。诊断和 diff 标记不进入剪贴板，也不
把草稿自动编码成删掉坏字段的版本。没有草稿的服务错误只通知失败；空响应不会抹去前一稿。
结果和诊断分别分片，完整组装后一次性交付，保留 Unicode 代理对。

修改继续以提交时的原稿为基准，聊天提供“查看修改”；未完成修订也能查看对比并复制原文。
聊天链接经客户端专用 `SpellAiClientCommandMixin` 将 `yhspellai` 命令交给已有客户端
dispatcher，再由客户端 tick 打开详情页。Minecraft 1.20.1 的点击路径使用
`sendUnsignedCommand`，绕过 Forge 仅接在 `sendCommand` 上的客户端命令处理；此前因此
误发服务器并提示“未知或不完整的命令”。修复只接管 `yhspellai`，不改其他命令的发送路径。
导入改变另一个已有 ID 时继续使用覆盖确认；当前 ID、新 ID、相同内容不新增弹窗，见
[编辑器修复](0.30.0pre1fix-editor.md)。生成、检查和差异展示均不注册、保存或激活符卡。

断线后客户端清理分片和对比记录，服务端停止后续修订且不投递结果。已发出的 provider 请求
仍可能完成，未实现离线保存/重连补投。后台 JSON 解析和修订调度不占用服务器 tick 的同步流程。

## 共用游戏检查逻辑

原 `RawJsonDockPanel` 的解析、Codec 往返和丢字段诊断提取为 `SpellJsonChecker`；
`SpellJsonSalvage` 移到 common definition 包，移除客户端限定。编辑器和 AI 使用同一实现，
没有新增独立 schema、Python 检查器、认证分析器或 EXP/OP 准入策略。

检查内容是 JSON 可读性、Codec 字段诊断和坏节点反馈。抢救保留可用兄弟节点，未知类型抛出的
异常也降级为坏节点；已有 `broken` 占位节点要求修订。`hold_source.on_release` 与激光回调
纳入嵌套诊断路径。旧面板会把部分显式默认值误报为不支持字段，本轮修正普通卡型/EX 标记、
发射器默认参数、反弹参数、当前碰撞默认值及 `size` → `base_scale` 读取别名的判断。

玩家提供的完整 Scarlet Gungnir 原稿复现了另一处误报：`tick_interval.offset=0` 是合法默认值，
Codec 编码时会省略它；旧检查器只识别 `.condition.offset`，遗漏
`.condition.conditions[1].offset`，从而在编辑器和 AI 回修中都报告不支持字段。
现按编码后的条件类型识别默认值，覆盖任意层级的 `and` / `or` / `not`，同时修正
`dynamic_tick_interval.offset`、`always.value` 和 `target_speed.op` 的同类遗漏。
动态间隔的零偏移支持数字、表达式字符串和 typed constant；真实未知字段和错误类型仍反馈原路径。
完整原稿已保存为测试资源，检查通过后 agent 只接收首稿就原样交付，不消耗两次回修机会。
此例的根因是反馈误报，无需删改合法 JSON 或调高修订次数。

此前导致 `experimental_fire policy EXPERIMENTAL` 拒绝的 `YhSpellDraftValidator` 保持删除。
通过内容检查不等于认证、可装备或实机效果验收；这些仍由玩家正常的认证和使用入口决定，
不以这些策略阻止创意草稿交付。

## Skill 来源与同步

先更新外部 `C:/Users/qq237/.codex/skills/yh-danmakuspell-create/`：入口收敛为内容创作流程；
`references/authoring.md` 保存花型、三维结构、mover、表达式时钟、修改和反馈修订指导；
`references/schema.md` 与例子按当前源码更新初始化名称、弹种、卡型、冻结/无敌时长、
`base_scale`、表现、来源/瞄准模式及回调字段。Python 验证器和两份旧规则文件已移除，
ECL 文档改用游戏诊断。旧 Skill 备份在本地 `build/pre1fix-check/skill-before-agent/`。

随后同步到模组：

```powershell
python tools/sync_spell_ai_skill.py C:/Users/qq237/.codex/skills/yh-danmakuspell-create
```

脚本只组合创作指南、字段参考和六个示例，输出
`src/main/resources/data/youkaishomecoming/llm/spell_generation_system.txt`；不检查符卡，构建也
不依赖外部 Skill。资源末尾记录源文件摘要，避免另维护一份手工提示词。提示词为 60,211 字节，
SHA-256：`dbf26ee95a1953a96eade2fd3584bea7076d3be93a6389fa88de71c57e509000`。
每次请求继续追加真实注册表产生的 `YhSpellCapabilityReport`；Skill 不作为 Java 工程契约。

## 路由与计费

common setup 注册 `YH_SPELL_GENERATION` 的 `PurposeMeta`，无需先启用 AI 或生成请求，控制台
即可发现用途；请求时幂等补注册。沿用 `SharedLlmRuntime.current()` 和控制台 purpose/default
路由。缺少可选库时不影响 YH 加载；计费 API 不兼容时明确报错，不发送未计费请求。

`spell_ai.maxOutputTokens` 默认 10,000 token。缺省/更低的 purpose 上限提升到这个基准，
更高的显式上限保留。完整请求构造后通过实际 runtime 的 `estimateWorstCaseBudget` 计算
凭据重试和 provider fallback 预算，始终使用请求玩家的 PLAYER 身份。

各轮有不同 request ID，共用首轮 ID 作为 causal root。llmcore 的链预算只能取旧值与新值的
较小者，不能在第二轮临时追加，因此首轮发送前即估算首轮及最多 N 次修订的总上限。
修订只保留原始要求、上一稿和最新诊断；后两者受编辑器既有 1,048,576 字符容量约束。
估算模板按最大 UTF-8 字符占用交给同一个核心 API；模板填充不发送给 provider，也不会
预扣虚拟输入。超出编辑器容量的返回稿保持原文交付并停止自动修订。
个人预算和实际结算由 llmcore 照常执行；不改第三方源码、不为修订重置 causal root。

## 验证

2026-09-13 的窄范围检查：

- `SpellJsonCheckerTest`：77 项；六个内置示例、完整 Scarlet Gungnir 原稿、不同层级的条件
  默认值、非默认值保留、真实错字段/错类型，以及非 JSON、坏节点抢救、表达式错误、读取别名
  和 EXP/OP 草稿不被拒绝。
- `YhSpellGenerationAgentTest`：17 项；有效首稿、完整原稿无需回修、错误反馈、修订上限、
  0 次修订、失败/空结果保稿、后台等待和断线停止。
- `YhLlmCoreBridgeTest`：实际 llm-core 1.4.4 下 51 项，缺库 3 项；loopback HTTP 复现旧拒绝，
  验证 fallback、连续三轮计费、同根不同 ID、UTF-8 最坏预算和 10,000 输出上限。
- `SpellAiDeliveryTest`：70 项；真实 L2Serial/FriendlyByteBuf 长 Unicode 编解码、交错请求、
  完整交付、草稿/诊断双分片、悬停反馈、请求原稿和差异数据。
- `SpellJsonOverwriteTest`：8 项；提取检查器后覆盖判断仍有效。

入口均为对应类的 `main`，使用项目 test runtime classpath；core 测试加入实际可选 jar，缺库
用例移除它并传 `absent`。两个内容测试用仓库内 `SpellTestBootstrap` 跳过 Forge mod 注册入口
的静态初始化，运行真实 Codec。日志在本地 `build/pre1fix-check/`。
条件默认值修复后仅重跑检查器和 agent 两组检查，其余组保留前轮成功记录；新原稿资源在
`src/test/resources/youkaishomecoming/spell/`，直接运行测试时需将 test resources 加入 classpath。
没有调用真实付费 provider、系统剪贴板或实际 GUI；生成质量、聊天/剪贴板和一局战斗待实机验收。

修复“查看修改”点击后重新通过上述 70 项交付检查。核对实际 Forge 1.20.1 点击字节码，
确认旧路径绕过客户端 dispatcher；新 JAR 的 mixin 仅列在 `client`，且 refmap 将
`sendUnsignedCommand` 正确映射到 `ClientPacketListener.m_246979_`。未启动游戏验证实际点击。

合并复核：`YoukaisHomecoming.java`、`build.gradle`、版本字段、语言生成器未改；改动集中在
fork 自有 llmcore 兼容与编辑器。中文修改分片后由 `organizeLang` 生成，英文维护运行时源。
common 检查器无客户端导入或 `OnlyIn`，公开跨模组 API 未改。客户端与服务端需更新同一 JAR，
以正确重组带诊断的结果分片。
点击修复仅新增 fork 自有客户端 mixin，并在现有 mixin 配置的 `client` 列表登记一项。

## 候选包

2026-09-13 专用服弹幕同步修复后的重建：`compileJava organizeLang --offline` 成功（1m 56s），随后 `jarJar --offline`
成功（12s），包含 `reobfJarJar` 与 `verifyJarJarRefmap`；两步均使用本页版本参数。分步运行
保证新生成的中文在打包前复制进资源目录。

JAR：`build/libs/youkaishomecoming-2.7.0+HCDRS_0.30.0pre1fix.jar`，9,492,750 字节。
SHA-256：`875011e0e6be1d595261bad19ef90774202f6a3846fe1794998881330426e6ca`。
已经复制到主工作区同名 `build/libs/` 路径并再次核对哈希。

ZIP CRC、版本、refmap、新 agent/common 检查器、包内提示词及 28 个相关语言键核对通过；
中文分片/生成文件/包内内容一致，“车万女仆”固定文案保留。旧 AI 草稿验证器、旧客户端
salvage 类和 post-pre2 结界运行时无残留，没有捆绑 llm-core。检查记录在
`build/pre1fix-check/async-artifact.json`，验包入口为本地 `verify-async.py --copy`。

条件默认值修复时额外核对包内 `SpellJsonChecker` 已包含条件类型判定、移除旧 `.condition.offset/value/op`
路径规则，测试原稿未打入运行包；复制后哈希一致。记录在 `build/pre1fix-check/nested-condition-artifact.json`。

当前包同时包含[失效草稿恢复](0.30.0pre1fix-editor.md#删除符卡后的草稿恢复)。该修复通过
34 项草稿恢复与 29 项战斗资格检查；额外核对物品使用、洗白配方和编辑器删除的修复调用均已
打包，原“查看修改”客户端 mixin 与 refmap 保留。记录在 `build/pre1fix-check/draft-recovery-artifact.json`。

当前包还包含[专用服弹幕同步修复](0.30.0pre1fix-danmaku-sync.md)。30 项客户端类不可用条件下的
包构造/序列化检查通过；包内公共同步类不再引用客户端世界与缓存，客户端 helper 的隔离标记
和反弹/停驻/继续飞行处理均已核对。记录在 `build/pre1fix-check/danmaku-side-artifact.json`。
