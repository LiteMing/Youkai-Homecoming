# 0.30.1：AI 符卡命令并入 /yhspell

0.30.1 将独立的 `/yhspellai` 收到 `/yhspell ai` 下。
命令整理以 `45c7ecb34` 独立提交，随后构建并安装到“归家之梦 ReSt”整合包。

## 玩家入口

| 命令 | 行为 |
|---|---|
| `/yhspell ai create` | 打开现有 AI 请求界面，预选“新建符卡”，以空白草稿为编辑器工作区。 |
| `/yhspell ai diff` | 查看当前连接中最近完成的 AI 修改。 |
| `/yhspell ai diff <request>` | 查看指定请求的修改；请求号支持当前可用记录的 Tab 补全。 |

新建命令复用编辑器的 AI 启用条件；输入需求并点击生成后，沿用现有后台请求、检查反馈、
聊天提醒和剪贴板交付。已有符卡的修改仍通过编辑器 AI 入口选择，默认保持“修改当前”。

“最近完成”按结果完成顺序更新，失败和新建请求不会替换已有修改对比。
没有可用记录时使用现有的对比不可用提示。退出连接时同时清理对比记录和待打开界面。

## 命令迁移

这是内部客户端命令路径的破坏性调整：移除 `/yhspellai diff <request>` 注册，
聊天“查看修改”链接统一生成 `/yhspell ai diff <request>`。

客户端注册 `/yhspell` 的 `ai` 子树，由 Forge 合并到服务端命令补全树。
`SpellAiClientCommandMixin` 只接管 `yhspell ai ` 前缀的聊天点击；其他 `/yhspell` 子命令
继续交由服务端处理。新建和对比页面都在客户端 tick 末尾打开，避免聊天发送后的关闭动作
覆盖刚打开的页面。

## 验证

- 4 个受影响 Java 文件使用 Java 17 和已有构建依赖定向编译通过。
- 现有 `SpellAiDeliveryTest` 的 70 项交付、分片和对比检查通过。
- 核对 Forge 1.20.1 的客户端命令合并及未知子命令回落逻辑；源码中旧命令注册和链接已移除。
- 沿用现有双语界面文案，没有新增语言键。

## 0.30.1 构建与安装

当前工作区存在已移除的结界和旧 AI 校验器编译文件；先运行 `cleanCompileJava`、
`cleanCompileTestJava`、`cleanProcessResources`、`cleanProcessTestResources` 清理输出。
随后 `compileJava organizeLang --offline --console=plain` 成功（1m 46s），
`jarJar --offline --console=plain` 成功（18s），包含重混淆和 refmap 验证。

产物：`build/libs/youkaishomecoming-2.7.0+HCDRS_0.30.1.jar`，9,493,006 字节。
SHA-256：`9c490adffb694e6852070501b0eaae095c4bed517b4dfcde728c9336a1ebf82f`。

ZIP CRC、版本、命令链接、客户端 mixin/refmap、双语资源一致性检查通过。
编译目录已无失去源码的顶层类；运行包中没有旧校验器或 aborted 结界类残留。

已安装到 `D:\.Minecraft\mchxx\.minecraft\versions\归家之梦ReSt\mods`，安装后哈希一致。
原 `0.30.0pre1fix` JAR 已移到同实例的 `.yh-mod-backups/0.30.1-20260913-235848/`。
本地构建和安装记录位于 `build/release-0.30.1/`。

本次没有启动游戏；命令补全、聊天点击和界面切换仍需实机回归。
