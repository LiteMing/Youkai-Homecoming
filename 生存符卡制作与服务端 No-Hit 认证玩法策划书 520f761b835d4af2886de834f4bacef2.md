# 生存符卡制作与服务端 No-Hit 认证玩法策划书

<aside>
🎯

本文是交给代码 Agent 的独立执行文档。目标分支为 `LiteMing/Youkai-Homecoming:hd-stable`。开始开发时必须重新记录 HEAD，并以实际源码为准。

</aside>

## 0. 开发前决策记录

以下决策以 HEAD `128b3e8af` 的真实源码核对结果为基础；若与后文旧描述冲突，以本节为准。P0～P7 实际为 **8 个阶段**。

| ID | 决策 | 落地要求 |
| --- | --- | --- |
| D1 | 认证弹幕使用服务端会话登记，不修改弹幕网络格式 | 每个认证会话维护弹幕 UUID 登记；派生 Shooter/Trail/hook 弹幕在生成时继承登记。不得每 tick 全表扫描，查询只放在生成、移除和实际碰撞入口，cleanup 直接清空会话登记。后续 profile 若发现 UUID 集合成本明显，再改为更紧凑的 server-only ID 结构。 |
| D2 | 普通 life 归零立即终止认证 | 作为兜底规则；更前置的规则是玩家已有普通弹幕战时不得开始认证，认证 ACTIVE 期间也不允许再加入另一个普通 Boss 弹幕战。 |
| D3 | ACTIVE 期间完全禁用手动 bomb 与玩家符卡 | 服务器在所有 bomb 和 `ISpellItem` 入口拒绝，不只拦截 `DynamicSpellItem`；不扣资源并返回明确提示。 |
| D4 | 认证期间禁用 life alpha 淡出 | 认证状态魔法阵固定 `globalAlpha = 1.0`；2.8 life 淡出曲线只用于普通弹幕战。bomb 子阵可显示真实数量，但应标记为封印/不可用。 |
| D5 | 第一版不引入 `spellSeed` | `SpellRuntime` 继续使用现有实体随机源；只锁定独立的 `movementSeed`。移动控制器必须使用自己的 `RandomSource`，不得消耗 `self().getRandom()`，避免随机移动改变符卡随机序列。 |
| D6 | `activeThreatTicks` 只影响折扣，不作为成功门槛 | 成功只要求 ACTIVE 存活达到 `targetTicks` 且未触发失败条件。若该 tick 存在至少一个可碰撞的认证弹幕/有效激光，或该 tick 生成了此类威胁，则计为 active threat；折扣使用有效威胁时间，空白尾段不获得完整时长折扣。 |
| D7 | AUTO_DODGE 第一版采用 best-effort 禁用 | 服务端下发认证状态，官方客户端关闭 AUTO_DODGE；服务器继续检查越界和异常位移，但不宣称能够证明合法速度内没有客户端自动闪避。真正服务器权威移动不属于 MVP。 |
| D8 | 两条碰撞路径统一进入幂等 Gateway | `IYHDanmaku.hurtTarget` 为主要接触入口；`YHAttackListener.onHurt` 作为 legacy/漏网兜底。二者调用同一个 `CertificationContactGateway`，按 session、projectile/来源和 tick 去重，任一路径都不能漏判或重复结算。 |
| D9 | legacy 预检必须先于序列化和 hash | 先执行结构资格扫描；发现 `LegacyTickerAction` 或其他不可稳定 JSON 往返内容立即拒绝。只有通过预检后才 canonical encode/decode 并生成 definition hash。 |
| D10 | P0 保留 `SpellMarketValidator.validate` 静态门面 | 新增共享 `SpellAnalyzer`，但旧静态方法签名尽量不变，内部委托 `SpellAnalyzer.analyze(definition, MARKET_PROFILE)`，减少市场导入调用方改动和回归面。 |
| D11 | 每名玩家同一时间最多一个认证 | `certificationMaxTrialsPerPlayer` 默认且 MVP 固定为 1；新 quote 使该玩家旧 quote 失效，服务器总并发上限另行配置。 |
| D12 | 认证使用专用进度 HUD，不显示普通血量 Boss Bar | 显示剩余时间、No-Hit 状态和失败原因；认证敌机不可击杀，因此普通血量 Boss Bar 没有语义。 |
| D13 | 认证弹幕只允许命中创建者 | 碰撞目标在认证 Gateway 前按 owner UUID 白名单过滤；认证敌机、其他玩家、其他妖怪均不能成为认证弹幕目标，也不产生 graze。必须单测认证敌机不会被自己的弹幕命中。 |
| D14 | 退款由结果分类决定，不使用单一布尔值 | `SYSTEM_ERROR`、生成失败和服务器保护中止始终全额退款且不可配置关闭；正常 No-Hit 失败与主动放弃分别使用 refund ratio/policy。 |
| D15 | 认证战与普通 Boss 弹幕战互斥 | PREPARE 前检查玩家没有普通弹幕战；ACTIVE 期间拒绝新的普通 Boss session。若仍因外部状态导致普通 life 归零，则按 D2 立即终止。 |
| D16 | 纯 JVM 单测范围受限（P0 实测） | spell codec 链（SpellActions→FireLaserAction→YHDanmaku→YoukaisHomecoming.<clinit>）初始化 mixin 补丁类（RecipeBookType.create 抛 "Enum not extended!"），无法在 FML/modlauncher/mixin 运行环境之外加载。**SpellAnalyzer 遍历数学、SpellHash、市场门面委托的自动化验证移到 Phase 2**（服务器内 `/yhdev spell_analyzer_self_test` 命令，P0.5 已实现）；纯逻辑部分（NumberBounds 求界规则、能力策略表）已有 `runSpellAnalyzerSelfTest` 覆盖（54 项断言）。 |
| D17 | P0 验收修复（P0.5，2026-08-08） | 按验收意见修复：① MARKET 恢复旧 raw-JSON 守卫（HistoricalMarketJsonGuard，历史消息逐字）+ Analyzer 补能力提取；② maxSpawnPerTick 计入一次性 tick 组（certMaxOneShotBurst）；③ Shooter 独立模型（无 window×lifetime 重复计费、并发峰值入 peakAlive）；④ Hook 子节点 multiplier 不再平方（childMult=executions）；⑤ Legacy 扫描统一到 SpellEligibility（覆盖 Burst/SpawnShooter/hook 列表，Analyzer 与 SpellHash 共用）；⑥ CERT 未知 Action fail-closed（unknown_action）；⑦ DisabledAction 规则明确：不计运行成本/能力，仍参与结构大小、深度、可序列化性与 legacy 检查；⑧ SpellHash 哈希前递归排序对象键（map/字段顺序无关，数组顺序保留）；⑨ Pow/Root 求界不可靠时返回 UNBOUNDED。 |
| D18 | P0 封板（2026-08-08，87/87 实机通过） | `/yhdev spell_analyzer_self_test` 在 runClient 实机 **87/87 全绿**。封板前累计修复：shooter 双模型（recurring cohort×window / one-shot lifetime）、同 tick 叠加（ordinary+shooter+entity）、on_hit 命中次数按 entity/block 各自行为（OR 合并曾膨胀 4 倍）、四层保守全加公式（ordinary + maxDirectOneShotEvent + delayedBurstSum + deferredSum，delay≤0 立即执行、transition edge exit+enter、跨组 deferred 全局相加）、循环 phase 生成 fail-closed（逐节点回达 SCC 检测，cross-edge 成员不漏）。**headless 自测**：`YHDEV_SELFTEST=1` + `YHDEV_SELFTEST_STOP=1` 启动 runServer 自动执行并写 `yhdev-selftest-result.txt`；**已知环境限制**：本仓库 dev 环境的 runServer 因依赖模组 mixin 的 client 类引用（VanillaPackResourcesBuilder → Minecraft）在 DEDICATED_SERVER 分发下崩溃，属上游/依赖问题，不影响本模组代码（本模组 mixin 无 client 引用）；实机验证以 runClient 单机 `/yhdev` 为准。 |

## 1. 项目目标

在生存模式中加入一套完整的玩家符卡创作、服务器认证、制作和施放闭环：

1. 玩家可在现有符卡编辑器中自由设计符卡。
2. 客户端预览负责粗验证、性能提示和报价预估，不具有认证权威性。
3. 玩家选择认证时长和三维场地范围，向服务器请求正式分析与报价。
4. 支付固定的认证启动费用后，候选符卡生成一个专用“认证敌机”。
5. 认证敌机继承 `YoukaiEntity`，使用候选 `SpellDefinition` 在服务器实际运行弹幕，与创建者建立专用弹幕战。
6. 创建者在规定范围内完成规定时长的 **No-Hit** 后，认证敌机消散并留下已认证符卡物品。
7. 成品符卡绑定不可变定义哈希、认证参数、能力需求和抽象成本；后续施放通过可替换支付系统消耗 bomb、points、life、经验或其他模组法力。
8. 弹幕战期间为玩家显示战斗魔法阵：阵内子阵表示玩家当前可用的 bomb/符卡数量；life 低于约 2.8 后，魔法阵整体随 life 减少逐渐降低不透明度。第一版先实现玩家侧，后续允许妖怪 Boss 以剩余符卡数显示子阵，并按血量曲线改变整体透明度。

## 2. 核心设计原则

### 2.1 创作自由不等于无限执行预算

- 编辑器允许复杂设计。
- 服务器始终执行能力检查和性能硬上限。
- 更高的资源消耗不能购买突破性能硬上限的权利。
- 超过 `maxSpawnPerTick`、`maxPeakAlive`、递归深度或服务器预算的符卡必须拒绝认证。

### 2.2 客户端只预估，服务器才签发证书

- 客户端预览结果不可作为成功凭证。
- 服务器重新解析定义、重新分析、生成 hash、锁定报价并运行认证战。
- 客户端显示的费用必须标注为“预估”；服务器返回的报价才是本次认证的正式报价。

### 2.3 No-Hit 以碰撞为真值，不以最终伤害为真值

认证失败条件定义为：

> 在认证 ACTIVE 阶段，认证敌机或其子 Shooter/Trail 所属的有效弹幕、激光，与创建者的规范弹幕战判定箱发生服务端碰撞。
> 

以下机制不得把一次碰撞变成认证成功：

- 自动 bomb；
- 无敌帧；
- 护甲、抗性、吸收；
- 其他模组护盾；
- Forge 伤害事件取消；
- 图腾；
- 最终没有扣除生命。

其他来源的伤害不影响认证：摔落、火焰、普通怪物、其他玩家、其他 Boss 和其他模组弹丸均不计入认证 No-Hit。

### 2.4 认证参数与成品绑定

- `certifiedDuration` 必须成为成品符卡的最大运行时长。
- 成品不得运行超过其认证时长。
- 认证场地范围、规则版本和 definition hash 写入证书。
- 修改定义后 hash 改变，旧证书立即失效，必须重新认证。

### 2.5 逻辑成本与支付资源解耦

内部只计算统一的 `SpellCostUnits` 和多维分析指标，不直接把符卡定义绑定为“3 级经验”或“1 bomb”。支付 Provider 根据当前环境进行换算。

## 3. 不在本项目范围内

- 不将整个弹幕系统重构为 ECS。
- 不重写现有客户端预览器和 Pilot。
- 不允许普通生存玩家使用 `EXPORT_GLOBAL` 覆盖全局符卡。
- 不允许认证解锁 `run_command`。
- 第一版不做完整的随机多 seed 难度评级。
- 第一版不做所有 Boss legacy ticker 的自动迁移。
- 第一版不做跨服务器共享证书。

## 4. 现有源码接入点

开发前必须重新阅读这些文件及其最新依赖：

### 定义与运行时

- `content/spell/definition/SpellDefinition.java`
- `content/spell/definition/PhaseDefinition.java`
- `content/spell/runtime/SpellRuntime.java`
- `content/spell/runtime/SpellContext.java`
- `content/spell/action/SpellActions.java`
- `content/spell/action/FireDanmakuAction.java`
- `content/spell/action/FireLaserAction.java`
- `content/spell/definition/OriginConfig.java`

### 预览与编辑

- `content/spell/preview/VirtualSpellScene.java`
- `content/spell/preview/PreviewCardHolder.java`
- `content/spell/preview/SpellPreviewScreen.java`
- `content/spell/preview/SpellEditorSyncToServer.java`
- `content/spell/preview/SpellEditorNetworkClient.java`

### 市场验证与存储

- `content/spell/market/SpellMarketValidator.java`
- `content/spell/runtime/CustomSpellStorage.java`
- `content/spell/runtime/SpellRegistry.java`

### 玩家施法与消耗

- `content/item/danmaku/DynamicSpellItem.java`
- `content/item/danmaku/SpellItemCost.java`
- `content/spell/item/SpellContainer.java`
- `content/entity/danmaku/DanmakuProxyEntity.java`
- `compat/kubejs/spell/DynamicSpellCastEventJS.java`

### 弹幕战与魔法阵

- `content/capability/GrazeCapability.java`
- `compat/stg/YHStgApi.java`
- `fastprojectileapi/spellcircle/EntitySpellCircleManager.java`
- `fastprojectileapi/spellcircle/SpellCircleWorldRenderer.java`
- `fastprojectileapi/spellcircle/SpellCircleLayer.java`
- `fastprojectileapi/spellcircle/SpellComponent.java`

## 5. 玩家体验流程

### 5.1 编辑阶段

玩家在现有编辑器中设计候选符卡。客户端实时显示：

- 预估生成总量；
- 预估峰值存活量；
- 预估服务端负担；
- 预估渲染负担；
- 所需实验能力；
- 客户端粗略费用；
- 明确的错误路径，例如 `phase/main/on_tick/2/on_trail/0`。

客户端允许普通预览、暂停、单步、倍速和 AI，但这些结果不计入正式认证。

### 5.2 请求正式报价

玩家点击“服务器认证”，选择：

- 认证时长；
- 三维场地半径或半边长；
- 支付 Provider（若服务器允许选择）；
- OYSM/默认敌机外观；
- 是否公开显示认证战。

建议默认预设：

- 时长：30 秒、60 秒、120 秒；
- 自定义范围：服务器配置允许 20～300 秒；
- 场地半边长：6、8、12、16 格；
- 自定义范围由服务器配置限制，禁止小于规范玩家判定箱所需最小范围。

服务器返回正式 `CertificationQuote`，界面必须分成两个面板：

1. **本次认证启动费用**：立即支付。
2. **成品符卡施放费用**：成功后写入符卡成本档案，不在此时默认扣除。

### 5.3 预备阶段

支付成功后：

- 服务器创建认证敌机；
- 显示场地边界；
- 显示敌机模型和魔法阵；
- 进行 100 tick 默认倒计时；
- 清理认证区域内上次残留的认证弹幕；
- 玩家可以在倒计时结束前取消，退款规则由启动费用策略决定。

### 5.4 正式认证

- 第一版保持 `SpellRuntime` 的现有实体随机源，不新增 `spellSeed`；服务器只为认证敌机运动生成并锁定独立的 `movementSeed`。
- 移动控制器使用独立 `RandomSource`，不得消耗认证敌机的 `self().getRandom()`，避免随机移动调用次数改变符卡本身的随机序列。
- `movementSeed` 在支付完成后由服务器生成，ACTIVE 前不向客户端提供完整运动轨迹；每次新挑战使用新的 seed，并在认证记录中保存，便于复现问题。
- 认证敌机执行服务器规定的平滑随机运动，玩家不能在候选符卡中修改其运动算法、速度上限或 waypoint 规则。
- 只计算真实服务器 tick；客户端卡顿不能加速认证。
- 玩家已有普通 Boss 弹幕战时不能进入 PREPARE；认证 ACTIVE 期间不允许再加入其他普通 Boss 弹幕战。
- 玩家越界、换维度、退出、死亡、普通 life 归零、非法瞬移或接触认证弹幕均失败。
- ACTIVE 期间服务器完全拒绝手动 bomb、自动 bomb、所有 `ISpellItem` 施放和针对认证弹幕的清除能力；拒绝时不扣资源并显示明确提示。
- 认证敌机不可被提前击杀。
- 若符卡自然结束早于认证时长，认证专用 Runtime 应重新开始同一符卡循环，直到达到目标时长。
- 只有存在有效威胁的 tick 计入 `activeThreatTicks`；空白尾段不能用于骗取长时间折扣。

### 5.5 成功表现

成功时按顺序执行：

1. 冻结 SpellRuntime。
2. 清理全部认证弹幕。
3. 魔法阵缩小、降低 alpha。
4. 认证敌机播放消散动画。
5. 中心生成高亮、无重力的认证符卡物品。
6. 物品仅创建者可拾取一段时间；背包允许时可在动画结束后自动吸入。
7. 若未领取、玩家退出或服务器重启，记录为待领取奖励。

### 5.6 失败表现

- 立即终止 Runtime 并清理认证弹幕。
- 显示明确失败原因：命中、越界、退出、非法移动、性能保护、服务器异常。
- 玩家失败与系统异常必须分开处理：服务器异常或性能分析实现错误应退款；正常 No-Hit 失败按启动费用策略结算。

## 6. 服务器认证实体

新增建议类：

```
content/entity/youkai/SpellCertificationEntity.java
content/spell/certification/CertificationController.java
```

`SpellCertificationEntity extends YoukaiEntity`，以兼容现有妖怪渲染、魔法阵和 OYSM，但必须覆盖或禁用：

- 普通战利品和经验；
- 好感、图鉴、击杀统计；
- 普通 Boss 进度和剧情；
- 普通选敌与仇恨；
- 其他玩家造成的伤害；
- 普通自然生成、永久保存和远距离移除语义；
- 普通胜利与失败结算；
- 普通对话、Boss 音乐和成就。

认证实体只允许：

- 绑定一个创建者；
- 运行一个不可变候选定义；
- 生成认证弹幕；
- 驱动认证状态机；
- 提供 OYSM/默认模型表现；
- 成功或失败后完整清理。

其他玩家默认：

- 可观看；
- 不会被认证弹幕伤害或擦弹；
- 不能攻击认证敌机；
- 不能清除、吸收或改变认证弹幕；
- 不能加入该认证会话。

## 7. 认证状态机

```
DRAFT
→ QUOTED
→ DEPOSIT_PAID
→ PREPARE
→ ACTIVE
→ SUCCESS | FAILED | ABORTED | SYSTEM_ERROR
→ REWARD | REFUND | CLEANUP
```

状态必须由服务器持有。建议 `CertificationController` 每次状态转换都验证前置条件，并提供统一 cleanup，避免实体被 `/kill`、卸载或异常移除后留下虚拟弹幕。

### ACTIVE 计时规则

- `elapsedTicks`：ACTIVE 状态实际经过的服务器 tick。
- `activeThreatTicks`：存在有效认证威胁的 tick。
- `targetTicks`：玩家选择并由服务器锁定的认证时长。
- 成功条件只要求 `elapsedTicks >= targetTicks` 且未出现命中、越界等失败条件。
- `activeThreatTicks` 不作为成功门槛，只用于计算认证时长折扣。
- 若该 tick 存在至少一个仍可碰撞的认证弹幕/有效激光，或该 tick 生成了此类威胁，则计为 active threat。
- 空白 tick 可以计入挑战经过时间，但不获得完整 proof discount。

## 8. No-Hit 真值与战斗隔离

新增专用认证碰撞入口，不依赖最终伤害结果：

```java
CertificationHitResult onCertificationProjectileContact(
    ServerPlayer player,
    SimplifiedProjectile projectile,
    UUID certificationId
);
```

要求：

- 使用 server-only 会话登记保存认证弹幕 UUID，不修改弹幕网络格式。
- 登记只在弹幕生成、移除、实际碰撞和 cleanup 使用，不允许每 tick 遍历 UUID 集合。
- `IYHDanmaku.hurtTarget` 与 `YHAttackListener.onHurt` 必须统一调用幂等的 `CertificationContactGateway`，前者为主要入口，后者为 legacy/漏网兜底。
- 在自动 bomb、无敌帧和普通伤害结算之前确认认证接触。
- 只接受当前 ACTIVE 认证的 projectile ownership/登记链。
- 子 Shooter、Trail、on-expiry、on-hit 派生弹幕在生成时继承 certificationId 并加入会话登记。
- 认证弹幕目标白名单只有创建者；其他玩家、妖怪和认证敌机本身不受伤、不擦弹。
- 同一接触从两条路径到达时按 session、来源和 tick 去重。
- 一次有效接触立即将状态改为 FAILED。
- Graze 不等于 Hit；擦弹允许。
- 认证期间禁止自动 bomb 把接触改为成功。

认证战不要直接复用普通 `performDanmakuHit()` 的资源扣除结果。应为 `GrazeCapability` 增加专用认证会话标识，认证碰撞只负责失败，不扣普通 life/bomb，也不触发普通 STG defeat。

## 9. 场地与移动规则

新增 `CertificationArena`：

```java
record CertificationArena(Vec3 center, double halfSize, Shape shape)
```

第一版使用固定 AABB，后续可扩展球形。

规则：

- 场地中心在 PREPARE 开始时锁定，不跟随敌机或玩家移动。
- 玩家越界直接失败；只允许极小浮点 epsilon，不将玩家强行 clamp 回场内。
- 敌机自身限制在场内，避免主动飞远降低密度。
- 玩家换维度或断线默认失败。
- 禁止通过末影传送、指令或其他模组瞬移绕过场地。
- 服务器记录每 tick 位移，超过配置阈值视为非法移动或瞬移。
- 官方客户端在认证状态下关闭本模组 AUTO_DODGE；服务器检查越界和异常位移，但第一版将其定义为 best-effort 限制，不宣称可以识别合法速度范围内的修改客户端自动闪避。

移动规范做成服务器策略：

```
CANONICAL：规范速度/判定箱，适合正式平衡。
MODPACK：允许合法装备和其他模组移动，但仍受越界和最大位移保护。
```

默认使用 `CANONICAL`。

### 9.1 认证敌机随机运动

认证敌机不能长期固定在一个位置，也不能对同一符卡始终使用公开且固定的飞行路线，以防玩家专门围绕固定轨迹制作“定制安定点”。

第一版采用服务器权威的**有界平滑随机 waypoint**：

- 在场地内部安全边距内随机选择目标点；
- 以受限加速度和最大速度平滑移动，禁止瞬移；
- 到达目标点或达到随机驻留时间后选择下一个目标点；
- waypoint 间隔、速度、加速度和边界安全距由服务器配置，不能由符卡定义修改；
- 轨迹不得根据玩家客户端输入预先公开；`movementSeed` 在支付后生成；
- 每次挑战使用新 seed，失败后重试不能沿用完全相同的固定轨迹；
- seed 与实际 waypoint 序列写入服务器认证日志，以便复现争议和调试；
- 随机运动仍必须遵守认证场地，不得因寻路失败停在边界外；
- 设定最低位移覆盖或最低 waypoint 次数，防止随机结果在长时间内接近静止；
- 运动算法应与候选 `SpellDefinition` 解耦，符卡只能读取认证敌机的真实位置和朝向，不能控制认证敌机移动。

该规则只防止针对固定敌机路线设计安定点，不承诺一次认证覆盖所有可能轨迹。后续可增加多 seed 高级认证，但不属于第一版 MVP。

## 10. 符卡分析器

从 `SpellMarketValidator` 中抽取共享分析核心：

```
content/spell/analysis/SpellAnalyzer.java
content/spell/analysis/SpellAnalysis.java
content/spell/analysis/SpellDiagnostic.java
content/spell/analysis/SpellCapability.java
content/spell/analysis/SpellCapabilityPolicy.java
```

建议结果：

```java
record SpellAnalysis(
    long totalSpawnUpperBound,
    long projectileTicks,
    int peakAliveUpperBound,
    int maxSpawnPerTick,
    long hookExecutionUpperBound,
    long expressionOps,
    double serverWork,
    double clientRenderWork,
    double gameplayPower,
    Set<SpellCapability> requiredCapabilities,
    List<SpellDiagnostic> diagnostics
) {}
```

### 必须计算的放大关系

- `on_tick` × phase 运行 tick；
- `repeat` × count；
- `burst` × count；
- `spawn_shooter` × shooter count × shooter lifetime × body；
- 弹幕 count × outerCount；
- `on_expiry`：每颗弹幕最多一次；
- `on_trail`：约 `lifetime / max(1, trailInterval)`；
- `on_hit_entity/on_hit_block`：默认采用“所有符合条件的弹幕都会触发”的保守上界；
- `HitBehavior.DISCARD` 默认每颗符合条件的弹幕最多触发一次；
- `HitBehavior.CONTINUE` 必须乘以每弹幕最大命中次数，且该次数具有服务器硬上限；
- on-hit 生成的新弹幕继续递归分析其 hook，直到定义结束或达到深度上限；循环或无法证明终止的 hook 链直接拒绝；
- Mover 组合深度和公式表达式复杂度；
- 激光的 active tick、长度、厚度和碰撞成本；
- 不同 DisplayType、bullet type、纹理层和 render bucket；
- 动态颜色、动态尺寸和动态 BulletProvider；
- phase transition 的循环和不可终止路径。

### Hook 成本计算模式

服务器提供两种分析模式；无论使用哪种经济计价模式，性能硬上限都必须继续执行递归保守分析。

#### `SIMPLIFIED_MULTIPLIER`（第一版默认）

只要定义中启用了某类条件 hook，就对符卡整体抽象成本叠加对应倍率，而不是尝试从一次客户端预览推断真实触发概率：

```
costAfterHooks = baseCost
               × onHitEntityMultiplier（若启用）
               × onHitBlockMultiplier（若启用）
               × onDiscardMultiplier（若启用）
               × 其他已配置 hook multiplier
```

规则：

- 同一 hook 类型是否按出现次数继续叠加，由服务器配置决定；默认同类型只应用一次全局倍率，避免单纯拆分 Action 造成不合理指数增长。
- 不同 hook 类型可以相乘。
- 嵌套 hook 仍要递归检查深度、循环、单 tick 生成和峰值存活，不能只乘费用后跳过安全分析。
- `on_expiry`、固定间隔 `on_trail` 等可确定次数的行为仍可直接统计，不必强制退化为概率估算。

#### `FULL_CONSERVATIVE`

按认证持续时间内的总弹幕生成量推导 hook 执行量：

```
onHitEntityExecutions
= eligibleProjectileCountDuringCertifiedDuration
× maxHitsPerProjectile
```

- 若 Preview 无法判断哪些弹幕实际命中实体，默认所有符合条件的弹幕都会命中并触发。
- on-hit 生成的弹幕继续进入总生成量，并递归计算其 on-hit、on-expiry、on-trail 等后续行为。
- 客户端可以额外显示“本次预览实际观察值”，但正式报价不能使用低于服务器保守值的观察结果。
- 无法推导总量、存在不受限 CONTINUE 命中或递归环时，拒绝认证而不是给出低估报价。

第一版实现顺序：先完成 `SIMPLIFIED_MULTIPLIER` 经济计价和保守安全上界；完整递归计数成熟后再开放 `FULL_CONSERVATIVE` 作为可选服务器模式。

### 序列化与 hash 前置资格扫描

在任何 canonical JSON 序列化和 definition hash 之前，先递归扫描整棵定义：

- 出现 `LegacyTickerAction` 立即拒绝，不能尝试先 hash；
- 其他不能稳定 JSON encode/decode 往返的节点同样拒绝；
- 通过预检后执行 canonical encode → decode 校验 → canonical hash；
- P0 保留 `SpellMarketValidator.validate` 的现有静态门面，内部改为委托共享 `SpellAnalyzer` 的 MARKET profile，尽量不改变现有调用方签名。

### 不可静态求界的表达式

- 若 count、lifetime、repeat、trailInterval 等关键数值无法推导上界，默认拒绝生存认证。
- 或要求执行路径在运行时应用服务器硬 clamp。
- 不允许只靠客户端 50,000 实体熔断保护服务器。

### 硬上限与成本分离

先检查：

- JSON/定义大小；
- phase/action/depth；
- 单 tick 生成量；
- 峰值存活量；
- 总 projectile-ticks；
- hook fanout；
- Shooter 数量；
- Runtime 子层级；
- 服务器实时 watchdog。

只有全部未超限，才计算资源成本。

## 11. 能力策略

能力策略必须检查 Action 类型和参数模式，递归遍历所有 phase、条件分支、Delay、Repeat、Burst、Shooter 和弹幕 hook。

| 能力 | 默认策略 | 说明 |
| --- | --- | --- |
| 基础 `fire_danmaku` / `fire_laser` | ALLOW | 受性能预算限制 |
| `on_expiry` / `on_trail` / `on_hit` | ALLOW | 必须计入 fanout 和硬上限 |
| `on_damage` | EXPERIMENTAL，默认 DENY | Boss 专用特性；第一版不认证此路径 |
| `OriginMode.TARGET` | EXPERIMENTAL，默认 DENY | Doremy 类敌方位置生成 |
| `OriginMode.ABSOLUTE` | EXPERIMENTAL，默认 DENY | 可能绕过相对场地语义 |
| `confine_target` | EXPERIMENTAL，默认 DENY | Koishi 类拉回/推拉能力 |
| `teleport` / `teleport_random` | EXPERIMENTAL，默认 DENY | 改变施法宿主位置 |
| `erase_enemy_danmaku` / `clear_screen` | EXPERIMENTAL，默认 DENY | 战斗资源与清弹能力 |
| `set_entity_flag` | EXPERIMENTAL，默认 DENY | 可能改变 Boss/玩家状态 |
| `force_spell` / `fire_spell` | EXPERIMENTAL，默认 DENY | 可绕过当前定义成本和递归限制 |
| `legacy_ticker` | DENY | 不可可靠序列化、分析和签名 |
| `run_command` | OP_ONLY | No-Hit 认证永远不能解锁 |
| `set_spell_circle` / `show_spell_title` / 安全音效 | ALLOW | 视觉表现仍受资源与 ID 白名单约束 |
| `ysm_render` | ALLOW/白名单 | 仅影响表现，不得改变碰撞箱 |

策略值：

```
ALLOW
EXPERIMENTAL
DENY
OP_ONLY
```

配置和脚本以稳定 capability ID 控制，不能依赖 Java 类名。认证开始时和每次施放时都重新检查当前策略。

## 12. `on_damage` 明确规则

- `PhaseDefinition.onDamage` 非空时，分析器添加 `boss_on_damage` capability。
- 默认生存策略为 DENY。
- UI 显示“包含 Boss 专用 on_damage，服务器未启用”。
- 实验性启用后，第一版仍按最坏情况计算其 fanout；不得因为认证战中没有触发而降低成本。
- 后续如需正式支持，应增加服务器按固定时刻触发的高级认证规则，不允许玩家通过不攻击敌机跳过危险分支。

## 13. 成本模型

不要直接用一个数字混合所有风险。先计算：

```
performanceCost
renderCost
logicCost
gameplayPowerCost
privilegeSurcharge
```

原始抽象成本：

```
rawCostUnits = base
             + f(performanceCost)
             + g(renderCost)
             + h(logicCost)
             + p(gameplayPowerCost)
             + privilegeSurcharge
```

认证折扣：

```
proofMultiplier = clamp(minProofMultiplier, 1.0,
    durationDiscount × arenaDiscount)
finalCostUnits = max(minimumCost, ceil(rawCostUnits × proofMultiplier))
```

要求：

- 认证时间折扣使用对数或平方根，收益递减。
- 场地折扣按半边长/半径计算，不按体积倒数，避免三次方放大。
- 推荐 `minProofMultiplier` 默认不低于 0.45。
- 符卡总运行时长增加原始成本；认证生存时长只降低 proofMultiplier。
- 空白 tick 不获得完整折扣。
- 性能硬上限不受折扣影响。

Remilia Gung 类高数量但结构清晰的弹幕：正常承担生成和渲染成本，不增加 Boss 惩罚能力附加费；其可读性通过更困难的长时长/小场地 No-Hit 自然获得折扣，不做硬编码特例。

## 14. 支付系统

重构 `SpellItemCost` 为可替换支付路由：

```java
interface SpellPaymentProvider {
    ResourceLocation id();
    boolean supports(ServerPlayer player, SpellCostContext context);
    PaymentQuote quote(ServerPlayer player, long costUnits, SpellCostContext context);
    PaymentResult tryPay(ServerPlayer player, PaymentQuote quote);
    void refund(ServerPlayer player, PaymentReceipt receipt);
}
```

上下文至少区分：

```
CERTIFICATION_START
CERTIFICATION_ISSUE
SPELL_CAST_STG
SPELL_CAST_NON_STG
```

默认 Provider：

- STG：Bomb Provider；
- 非 STG：Experience Provider；
- Points Provider：可作为启动费或抵扣；
- Life Provider：仅作为配置开启的高成本“超载施放”，不得默认悄悄扣 life；
- 外部法力 Provider：由脚本或兼容模组注册，例如新生魔艺法力。

KubeJS/脚本事件必须允许：

- 替换默认 Provider；
- 修改报价；
- 标记 `handled`；
- 返回支付失败原因；
- 执行退款。

当前 `DynamicSpellCastEventJS` 只能取消，之后仍会执行默认 `SpellItemCost.tryPay()`，因此不足以实现外部法力完全替代经验，必须扩展或新增支付事件。

### 启动费用建议

- 启动费用主要防止认证实体刷屏，不应默认扣 life。
- 可使用少量 points、经验、外部法力或未来的空白符卡材料。
- 正常失败是否损失、成功是否抵扣制成费用进入服务器配置。
- `SYSTEM_ERROR` 必须全额退款。

### 成品施放报价

界面应分别显示：

```
弹幕战：Bomb X；可选 Points 抵扣；Life 超载（若启用）
非弹幕战：XP X；或当前外部法力 Provider Y
```

不要默认同时扣 bomb、life、points。Life 必须由玩家明确选择或服务器规则明确声明。

## 15. 不可变存储与符卡证书

不要让生存认证复用 `EXPORT_GLOBAL`。新增世界级存储：

```
<world>/youkaishomecoming_certified_spells/<hash>.json
```

建议证书：

```java
record SpellCertificate(
    UUID certificateId,
    String definitionHash,
    UUID authorId,
    String authorName,
    int certifiedDuration,
    double certifiedArenaHalfSize,
    String arenaShape,
    long costUnits,
    Set<SpellCapability> capabilities,
    int analysisVersion,
    int certificationRulesVersion,
    long issuedAtGameTime
) {}
```

成品物品至少保存：

```
certificate_id
definition_hash
spell_id/revision
author_uuid
certified_duration
cost_units
```

服务器施放时：

1. 读取证书。
2. 验证 hash 对应的不可变定义。
3. 检查能力策略仍允许。
4. 使用证书的 `certifiedDuration` 限制 DynamicSpellItem proxy。
5. 通过当前支付 Provider 报价和扣费。

旧证书的迁移策略：分析规则大版本改变时，可配置继续有效、重新认证或仅禁止有安全问题的能力。

## 16. 奖励物品安全

成功生成的符卡物品应：

- glowing；
- noGravity；
- invulnerable；
- 仅作者可拾取一段时间；
- 延长或禁止自然 despawn；
- 火焰、爆炸、仙人掌不销毁；
- 背包有空间时可在表现完成后自动加入；
- 背包满时保留世界物品；
- 玩家离线或服务器重启时进入 pending reward 存储。

视觉实体与真实奖励可以分离：先生成纯表现符卡，动画结束后服务器安全发放真实物品，避免掉落实体丢失。

## 17. 魔法阵玩法

### 17.1 两类魔法阵

- **符卡临时魔法阵**：继续附着 `DanmakuProxyEntity`，由 `SetSpellCircleAction` 控制，随本次符卡结束。
- **角色弹幕战状态魔法阵**：附着实际角色实体。第一版只实现 Player，由 `GrazeCapability` 或专用视觉 capability 控制；后续扩展到妖怪 Boss。

### 17.2 子阵的明确语义

魔法阵内的子阵不是纯装饰，也不是简单的 bomb 百分比条，而是角色当前可用 **bomb/符卡数量** 的离散显示：

- 玩家第一版：一个完整子阵对应 1 个完整 bomb/符卡单位。
- 当前内部 bomb 以 `RESOURCE_UNIT = 5` 表示 1.0，因此 raw 值换算为 `wholeUnits + fractionalUnit`。
- 例如 2.4 bomb：显示 2 个完整子阵，并让第 3 个子阵按 0.4 的比例显示或改变 alpha。
- 消耗 1 bomb 后，对应子阵消失；获得 bomb 后按固定槽位顺序重新出现。
- 子阵槽位使用稳定标识，例如 `resource_slot_0...N`，不能依赖数组偶然顺序。
- 超过魔法阵设计最大槽位时，可以显示上限槽位并增加简洁的溢出提示；不得无限创建渲染层。
- 第一版只读取玩家 `GrazeCapability` 的 bomb/符卡资源，不实现 Boss 子阵逻辑。
- 后续 Boss 扩展：一个子阵对应一个剩余符卡/阶段，进入下一张符卡后消失一个，具体映射可由 Boss 配置覆盖。

建议状态：

```java
record SpellCircleResourceState(
    ResourceKind kind,      // PLAYER_BOMB，未来 BOSS_SPELL_CARD
    int wholeUnits,
    float fractionalUnit,
    int maxVisibleUnits
) {}
```

### 17.3 玩家 life 与整体透明度

第一版不再以“整体变暗”为主要表现，而是使用**全局 alpha**：

- 玩家 life 约 3.0 及以上时，魔法阵完全不透明，`globalAlpha = 1.0`。
- `fadeStartLife` 默认设为 2.8；life 低于 2.8 后开始连续降低不透明度。
- 不做 2.8、2.0、1.0 的突变档位，客户端使用平滑曲线和插值，避免资源同步时闪烁。
- 建议第一版曲线：

```
life >= fadeStartLife:
    globalAlpha = 1.0
life < fadeStartLife:
    t = clamp(life / fadeStartLife, 0, 1)
    globalAlpha = minAlpha + (1 - minAlpha) × smoothstep(t)
```

- `minAlpha` 默认建议 0.10～0.20，最终由实机视觉测试确定；life 为 0 时仍可保留极淡轮廓，也可在战斗结束动画中再完全消失。
- alpha 乘数应用于整个角色状态魔法阵，包括外环、纹理、stroke 和仍然存在的资源子阵。
- bomb 子阵的槽位 alpha 先按完整/碎片状态计算，再乘全局 life alpha。
- 仅 life/bomb 实际变化时同步目标状态，客户端逐帧插值；禁止每 tick 写 persistent data 或发送完整魔法阵定义。
- `SpellComponent.RenderHandle` 增加全局 alpha 乘数，优先使用顶点颜色 alpha，不使用全屏后处理。

建议组合状态：

```java
record SpellCircleAppearance(
    float lifeUnits,
    float globalAlpha,
    SpellCircleResourceState resource,
    int enabledChannels
) {}
```

### 17.4 妖怪 Boss 的后续扩展

Boss 不属于第一版交付，但底层状态不应写死为 Player：

- Boss 子阵可表示剩余符卡数量或剩余阶段数。
- Boss 整体透明度可按当前血量、当前阶段最大血量或独立脚本曲线变化。
- 不强制 Boss 使用玩家的 `2.8 life` 阈值；Boss 配置可定义 `fullAlphaHealthRatio`、`fadeCurve` 和 `minAlpha`。
- 阶段切换时子阵减少和 alpha 变化可配合动画，但不应修改实际战斗数值。

### 17.5 认证状态表现

- 普通弹幕战使用 2.8 life 以下的 alpha 淡出曲线。
- 认证 PREPARE/ACTIVE 期间玩家魔法阵固定 `globalAlpha = 1.0`，不读取认证接触来减少 life。
- bomb/符卡子阵仍可显示玩家真实库存，但必须提供封印/不可用的视觉状态，避免玩家误以为认证中可以施放。
- 认证敌机不显示普通血量 Boss Bar；使用专用 HUD 显示剩余时间、No-Hit 状态和失败原因。

### 17.6 第一人称

当前 `SpellCircleWorldRenderer` 会跳过第一人称相机实体。必须明确实现：

- 第三人称和其他玩家可见；
- 第一人称若需显示，增加专用第一人称渲染或 HUD；
- 不要意外重复渲染 Player 和 Proxy 两套魔法阵。

## 18. 网络协议建议

新增独立认证协议，不复用 OP 编辑包：

```
CertificationQuoteRequestToServer
CertificationQuoteToClient
CertificationStartRequestToServer
CertificationStateToClient
CertificationAbortRequestToServer
CertifiedSpellRewardToClient
```

要求：

- 报价请求上传完整候选 JSON 或服务端 draft ID。
- 服务器解析后返回 hash 和正式报价。
- Start 请求必须携带 quote ID/hash，防止客户端替换定义。
- Quote 有有效期，并在服务器配置或能力策略变化后失效。
- 所有参数由服务器再次 clamp。
- 客户端不能自行指定成功、计时或命中结果。

## 19. 实时性能保护

即使静态分析通过，认证运行时仍需 watchdog：

- 当前认证弹幕数；
- 本 tick 生成量；
- projectile-ticks；
- Shooter 数；
- hook 次数；
- 连续若干 tick 的认证 Runtime 耗时。

处理原则：

- 超过硬数量上限立即 `SYSTEM_ERROR/REJECTED_RUNTIME_LIMIT`，清理并记录诊断。
- 单 tick 偶发耗时不能直接判玩家失败；应采用连续窗口。
- 系统保护触发默认退款启动费用，并把实际指标写入日志。
- 不允许认证实体破坏全局现有 `ParallelTicker`、UserMatrixCache 和批量渲染优化。

## 20. 配置项

建议加入服务器配置：

```
certificationEnabled
certificationMinDurationTicks
certificationMaxDurationTicks
certificationDurationPresets
certificationMinArenaHalfSize
certificationMaxArenaHalfSize
certificationArenaPresets
certificationCountdownTicks
certificationMovementPolicy
certificationMaxDisplacementPerTick
certificationEnemyRandomMovementEnabled
certificationEnemyWaypointMinTicks
certificationEnemyWaypointMaxTicks
certificationEnemyMaxSpeed
certificationEnemyAcceleration
certificationEnemyBoundaryMargin
certificationEnemyMinimumTravelDistance
certificationMaxConcurrentTrials
certificationMaxTrialsPerPlayer
certificationStartCostUnits
certificationFailureRefundRatio
certificationAbortRefundRatio
certificationMinProofMultiplier
certificationMaxSpawnPerTick
certificationMaxPeakAlive
certificationMaxProjectileTicks
certificationMaxHookExecutions
certificationRuntimeWatchdogNanos
spellAnalysisHookCostMode
onHitEntityCostMultiplier
onHitBlockCostMultiplier
onDiscardCostMultiplier
spellCirclePlayerFadeStartLife
spellCirclePlayerMinAlpha
spellCircleMaxResourceSubCircles
certificationPublicRendering
certificationRewardOwnerLockTicks
certificationRewardNeverDespawn
```

能力策略应有独立配置或命令，不与数值上限混在一起。`SYSTEM_ERROR`、生成失败和服务器保护中止始终全额退款，不提供关闭该退款的配置。`certificationMaxTrialsPerPlayer` 的 MVP 默认值为 1。

## 21. 命令与脚本

建议管理员命令：

```
/yhspell certification enable|disable
/yhspell certification abort <player>
/yhspell certification inspect <player>
/yhspell certification claim
/yhspell capability get <id>
/yhspell capability set <id> allow|experimental|deny|op_only
/yhspell certificate inspect <held|id>
/yhspell certificate revoke <id>
```

KubeJS/脚本接口：

- 查询 SpellAnalysis；
- 修改/拒绝报价；
- 注册 PaymentProvider；
- 修改 capability policy；
- 监听认证开始、成功、失败、奖励领取；
- 不允许脚本客户端直接签发证书，签发始终由服务器执行。

## 22. 兼容与迁移

- 保留现有 OP `SAVE/EXPORT_GLOBAL/DELETE` 工作流。
- 市场导入和生存认证共享 `SpellAnalyzer`，但使用不同 policy/profile。
- 现有非认证 DynamicSpellItem 保持兼容，可继续使用旧固定成本，后续逐步迁移。
- 已认证物品走 certificate 路径，不允许回退为仅按 mutable `spell_id` 加载。
- OYSM 模型只改变视觉，不得改变认证敌机或玩家碰撞箱。
- 模型不存在时使用固定 fallback 模型，不能阻止认证逻辑清理。

## 23. 测试计划

### 23.1 单元测试

- 相同定义生成稳定 hash。
- 修改任意影响运行的字段后 hash 改变。
- Repeat/Burst/Trail/Shooter 放大计算正确。
- `SIMPLIFIED_MULTIPLIER` 在启用 on-hit 等 hook 时正确叠加整体倍率。
- `FULL_CONSERVATIVE` 在无法判断实际命中时按所有符合条件弹幕都会触发计算。
- on-hit 派生弹幕继续递归计数，循环或不受限 CONTINUE 被拒绝。
- `on_damage` 默认产生实验能力并被拒绝。
- `LegacyTickerAction` 在 canonical JSON/hash 之前被拒绝。
- TARGET/ABSOLUTE origin 默认被拒绝。
- `run_command` 永远不能被普通认证允许。
- 不可求界 NumberProvider 被拒绝或 clamp。
- proofMultiplier 有下限且收益递减。
- 空白 tick 不产生完整时长折扣。
- PaymentProvider quote/pay/refund 事务正确。

### 23.2 集成测试

- 支付启动费后正确生成认证敌机。
- 倒计时期间无有效弹幕判定。
- 玩家已有普通 Boss 弹幕战时不能开始认证；认证 ACTIVE 期间不能加入新的普通 Boss 战。
- 认证弹幕从 `IYHDanmaku.hurtTarget` 与 `YHAttackListener.onHurt` 两条路径都能判定，重复到达只结算一次。
- 认证弹幕碰撞在自动 bomb 之前判失败。
- 无敌、护盾、伤害取消不能阻止失败。
- 其他来源伤害不导致失败。
- ACTIVE 期间手动/自动 bomb、所有 `ISpellItem` 和清弹入口均被服务器拒绝且不扣资源。
- 其他玩家、妖怪和认证敌机本身不受认证弹幕影响，也不产生 graze。
- 越界、换维度、断线正确失败并清理。
- 认证敌机使用服务器随机平滑路线，不会长期固定；同一符卡的新挑战使用不同 movement seed。
- movement seed 和 waypoint 日志可用于复现，客户端不能预先控制路线。
- 成功后 Runtime 停止、弹幕清空、敌机消散、奖励发放。
- 背包满、离线和重启后奖励可领取。
- 成品运行时间不超过 certifiedDuration。
- 修改世界存储定义不能绕过 hash。
- 能力认证后被管理员关闭时，成品施放被正确拒绝并提示。

### 23.3 性能测试

至少覆盖：

- 1 个认证敌机 × 2k/10k/接近上限弹幕；
- 多玩家同时认证；
- 大量 on_trail；
- 多 Shooter；
- 激光与透明/叠加渲染混合；
- 成功/失败 cleanup 后无残留实体、虚拟弹幕和网络 erase 队列。

### 23.4 人工 runClient 验收

- 编辑器预估界面可读。
- 正式报价与客户端预估差异有明确说明。
- 场地边界清晰。
- OYSM 模型正常，fallback 正常。
- 命中、越界、成功反馈明确。
- 敌机消散和无重力高亮符卡表现完整。
- 玩家每个完整 bomb/符卡单位对应一个子阵，碎片单位正确表现为部分子阵。
- life 在约 3.0 及以上时魔法阵不透明，低于 2.8 后连续降低全局 alpha，资源同步时无闪烁。
- 第一/第三人称行为符合设计。

## 24. 分阶段执行计划

### Phase 0：分析器与能力策略

产出：共享 `SpellAnalyzer`、诊断、policy、legacy/不可序列化前置扫描和 canonical hash 工具；保留 `SpellMarketValidator.validate` 静态门面并让其委托 MARKET profile。

验收：单元测试通过；legacy 在 hash 前拒绝；现有市场导入调用方和行为不回归。

### Phase 1：支付解耦

产出：`SpellPaymentProvider`、默认 bomb/XP/points/life 路由、KubeJS handled 机制、事务回执。

验收：现有 SpellItem/DynamicSpellItem 默认行为保持兼容；外部 Provider 可完全替代 XP。

### Phase 2：服务器认证后端

产出：认证实体、Controller、状态机、arena、独立 movement RNG、server-only 弹幕 UUID 会话登记、双碰撞入口幂等 Gateway、正式报价、启动支付、No-Hit 服务端真值和 cleanup。

验收：命中/越界/退出/成功路径均可通过命令或最小 UI 重跑；不修改弹幕网络格式；普通 Boss 战与认证互斥。

### Phase 3：客户端正式认证 UI

产出：编辑器分析面板、参数选择、服务器报价确认、倒计时和状态 HUD。

验收：客户端不能自行声明成功；所有参数以服务器返回为准。

### Phase 4：证书、不可变存储与奖励

产出：hash 存储、证书、成品 NBT、奖励动画、pending reward、施放时验证。

验收：定义覆盖、NBT 修改和旧 quote 均不能绕过验证。

### Phase 5：玩家战斗魔法阵

产出：玩家 STG 魔法阵生命周期、bomb/符卡数量子阵、碎片子阵表现、life 低于 2.8 后的全局 alpha 曲线、第一人称方案，并为未来 Boss 血量/剩余符卡扩展保留通用状态。

验收：3 life 左右及以上保持不透明，低于 2.8 后连续变透明；完整 bomb 与碎片数量正确映射到子阵；资源变化时不进行每 tick 网络写入。

### Phase 6：脚本、命令与兼容

产出：capability 命令、KubeJS 支付与认证事件、OYSM 白名单/fallback。

验收：脚本可替换非 STG XP 为外部法力；不能客户端签发证书。

### Phase 7：平衡与压力测试

产出：配置默认值、Remilia Gung 等基准符卡报价、并发认证压力报告。

验收：提交实际测试数据，不以静态猜测宣称平衡完成。

## 25. 代码 Agent 执行要求

1. 每个 Phase 独立提交，不跨阶段一次性大改。
2. 每个 Phase 开始前重新阅读相关实际源码和近期提交。
3. 不重写现有弹幕热路径，不破坏批量网络、ParallelTicker、UserMatrixCache 和渲染分桶。
4. 新增递归分析时必须有深度和溢出保护，所有乘法使用饱和运算。
5. 所有客户端输入在服务器重新 clamp 和验证。
6. 所有实体终止路径必须统一 cleanup，并测试 `/kill`、卸载、断线、换维度和服务器停止。
7. 涉及页面、UI、渲染的人工验收必须标记“待人工”，不得伪称已经实测。
8. 每阶段执行 `gradlew build`；若存在独立测试任务，同时运行并记录结果。
9. 新增配置必须有注释和合理默认值，禁止散落魔法数字。
10. 文档与实际代码冲突时以代码为准，并同步更新本策划书或实施记录。

## 26. 最终 MVP 验收标准

MVP 完成必须同时满足：

- [ ]  普通玩家可以从编辑器提交候选符卡，但不能获得 OP 编辑权限。
- [ ]  服务器能拒绝超预算、危险能力和默认禁用的 `on_damage`。
- [ ]  玩家可以选择时长与场地并看到正式锁定报价。
- [ ]  支付启动费用后生成专用认证敌机。
- [ ]  认证敌机继承 YoukaiEntity 并可使用 OYSM/fallback 模型。
- [ ]  No-Hit 由服务端碰撞判定，自动 bomb、护盾和伤害取消不能绕过。
- [ ]  其他来源伤害和其他玩家不会污染认证结果；普通 Boss 战与认证不能同时存在。
- [ ]  ACTIVE 期间手动/自动 Bomb、所有玩家符卡和清弹入口均被服务器拒绝且不扣资源。
- [ ]  两条现有碰撞路径均进入同一个幂等认证 Gateway，不漏判也不重复结算。
- [ ]  认证弹幕只命中创建者，不命中其他玩家、妖怪或认证敌机自身。
- [ ]  越界、退出和换维度正确失败并无残留。
- [ ]  达成时长后敌机消散并安全发放高亮无重力符卡。
- [ ]  成品绑定不可变 hash 和 certifiedDuration。
- [ ]  成品每次施放重新检查能力策略并通过支付 Provider 扣费。
- [ ]  默认支持 STG bomb 与非 STG XP，脚本可完全替换为外部法力。
- [ ]  玩家普通 STG 魔法阵以子阵显示完整及碎片 bomb/符卡数量；约 3 life 及以上保持不透明，低于 2.8 life 后随 life 减少连续降低全局 alpha；认证期间固定全 alpha 并显示资源封印状态。
- [ ]  认证敌机执行服务器权威的有界平滑随机运动，新挑战使用新 movement seed，玩家不能通过符卡定义固定或控制其路线。
- [ ]  on-hit 等条件 hook 在简化模式下叠加整体成本倍率；完整模式无法判断命中时按所有符合条件弹幕都会触发计算。
- [ ]  所有关键路径有自动测试或明确的人工验收记录。